#recent_articles.sidebar_child
  %h3 Recent Articles
  %ul
    - for article in Article.all
      %li
        %h4= link_to article.title, article, :remote => true
        - unless article == Article.all.last
          .divider